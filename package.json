{
  "name": "task-management-app",
  "version": "1.0.0",
  "description": "Team Task Management App with Pomodoro Timer",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "prisma generate && prisma db push && cd client && npm install && npm run build"
  },
  "dependencies": {
    "@prisma/client": "^5.10.2",
    "express": "^4.18.3",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "prisma": "^5.10.2"
  }
}
